<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="android.view.View"/>
        <import type="de.hicedevelopments.princemusicapp.ui.artistdetail.ArtistDetailViewModel.ArtistDetailEvent"/>
        <variable
            name="viewModel"
            type="de.hicedevelopments.princemusicapp.ui.artistdetail.ArtistDetailViewModel" />
    </data>
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/coordinator"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="false">
        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appbar"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            android:backgroundTint="@color/purple"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <com.google.android.material.appbar.CollapsingToolbarLayout
                android:id="@+id/collapsing_toolbar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:contentScrim="@color/purple"
                app:expandedTitleGravity="bottom"
                app:expandedTitleTextColor="@android:color/transparent"
                app:collapsedTitleTextColor="@color/gold"
                app:title="@{viewModel.artistInfo.name}"
                app:titleCollapseMode="fade"
                app:layout_scrollFlags="scroll|exitUntilCollapsed">
                <androidx.constraintlayout.widget.ConstraintLayout
                    android:id="@+id/header_container"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:layout_collapseMode="parallax">
                    <androidx.appcompat.widget.AppCompatImageView
                        android:id="@+id/header"
                        android:layout_width="match_parent"
                        android:layout_height="0dp"
                        android:scaleType="centerCrop"
                        app:imageUrl="@{viewModel.artistInfo.images.get(0).uri}"
                        app:showPlaceholder="@{true}"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintDimensionRatio="H, 1:1"/>
                </androidx.constraintlayout.widget.ConstraintLayout>
                <androidx.constraintlayout.widget.ConstraintLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:onClick="@{() -> viewModel.onEvent(ArtistDetailEvent.IMAGE_CLICK)}">
                    <androidx.appcompat.widget.LinearLayoutCompat
                        android:layout_width="match_parent"
                        android:layout_height="0dp"
                        app:layout_constraintDimensionRatio="H, 1:1"
                        android:gravity="bottom"
                        android:padding="@dimen/padding_large"
                        android:background="@drawable/release_detail_title_bg"
                        app:layout_constraintBottom_toBottomOf="parent"
                        android:orientation="vertical">
                        <androidx.appcompat.widget.AppCompatTextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:visibility="@{viewModel.artistInfo == null ? View.GONE : View.VISIBLE}"
                            android:textSize="@dimen/font_large"
                            android:textColor="@color/gold_70"
                            android:textStyle="bold"
                            android:text="@{viewModel.artistInfo.name}"
                            tools:text="Artist name" />
                    </androidx.appcompat.widget.LinearLayoutCompat>
                    <androidx.appcompat.widget.AppCompatImageView
                        android:id="@+id/imv_zoom"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_margin="@dimen/margin_large"
                        android:visibility="@{viewModel.artistInfo.images.size() > 1 ? View.VISIBLE : View.GONE}"
                        app:layout_constraintBottom_toBottomOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"
                        android:src="@drawable/ic_zoom_image" />
                </androidx.constraintlayout.widget.ConstraintLayout>
                <androidx.appcompat.widget.Toolbar
                    android:id="@+id/toolbar"
                    android:layout_width="match_parent"
                    android:layout_height="?attr/actionBarSize"
                    android:elevation="@dimen/toolbar_elevation"
                    android:theme="@style/PrinceToolbar"
                    app:useAsBackButton="@{true}"
                    app:navigationIcon="@drawable/ic_circular_back"
                    app:layout_collapseMode="pin"/>
            </com.google.android.material.appbar.CollapsingToolbarLayout>
        </com.google.android.material.appbar.AppBarLayout>
        <androidx.core.widget.NestedScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:overScrollMode="ifContentScrolls"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">
            <androidx.appcompat.widget.LinearLayoutCompat
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">
                <androidx.appcompat.widget.LinearLayoutCompat
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingBottom="@dimen/padding_normal"
                    app:layout_constraintTop_toTopOf="parent"
                    android:orientation="vertical"
                    android:visibility="@{viewModel.artistInfo.realName == null ? View.GONE : View.VISIBLE}">
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:background="@drawable/release_video_title_bg"
                        android:layout_marginVertical="@dimen/margin_normal"
                        android:padding="@dimen/padding_medium"
                        android:textSize="@dimen/font_large"
                        android:textColor="@color/gold_70"
                        android:textStyle="bold"
                        android:lineSpacingExtra="0sp"
                        android:text="@string/artist_real_name"/>
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginVertical="@dimen/margin_normal"
                        android:layout_marginHorizontal="@dimen/padding_medium"
                        android:textSize="@dimen/font_normal"
                        android:textColor="@color/white"
                        android:text="@{viewModel.artistInfo.realName}"
                        tools:text="artist real name"/>
                </androidx.appcompat.widget.LinearLayoutCompat>
                <androidx.appcompat.widget.LinearLayoutCompat
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingBottom="@dimen/padding_normal"
                    app:layout_constraintTop_toTopOf="parent"
                    android:orientation="vertical"
                    android:visibility="@{viewModel.artistInfo.realName == null ? View.GONE : View.VISIBLE}">
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:background="@drawable/release_video_title_bg"
                        android:layout_marginVertical="@dimen/margin_normal"
                        android:padding="@dimen/padding_medium"
                        android:textSize="@dimen/font_large"
                        android:textColor="@color/gold_70"
                        android:textStyle="bold"
                        android:lineSpacingExtra="0sp"
                        android:text="@string/artist_name_variations"/>
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginVertical="@dimen/margin_normal"
                        android:layout_marginHorizontal="@dimen/padding_medium"
                        android:textSize="@dimen/font_normal"
                        android:textColor="@color/white"
                        app:textList="@{viewModel.artistInfo.nameVariations}"
                        tools:text="artist real name"/>
                </androidx.appcompat.widget.LinearLayoutCompat>
                <androidx.appcompat.widget.LinearLayoutCompat
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingBottom="@dimen/padding_normal"
                    app:layout_constraintTop_toTopOf="parent"
                    android:orientation="vertical"
                    android:visibility="@{viewModel.artistInfo.profile == null ? View.GONE : View.VISIBLE}">
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:background="@drawable/release_video_title_bg"
                        android:layout_marginVertical="@dimen/margin_normal"
                        android:padding="@dimen/padding_medium"
                        android:textSize="@dimen/font_large"
                        android:textColor="@color/gold_70"
                        android:textStyle="bold"
                        android:text="@string/artist_profile"/>
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginVertical="@dimen/margin_normal"
                        android:paddingHorizontal="@dimen/padding_medium"
                        android:textSize="@dimen/font_normal"
                        android:textColor="@color/white"
                        android:text="@string/artist_about"
                        tools:text="artist profile"/>
                </androidx.appcompat.widget.LinearLayoutCompat><androidx.appcompat.widget.LinearLayoutCompat
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingBottom="@dimen/padding_normal"
                app:layout_constraintTop_toTopOf="parent"
                android:orientation="vertical"
                android:visibility="@{viewModel.artistInfo.urls == null ? View.GONE : View.VISIBLE}">
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@drawable/release_video_title_bg"
                    android:layout_marginVertical="@dimen/margin_normal"
                    android:padding="@dimen/padding_medium"
                    android:textSize="@dimen/font_large"
                    android:textColor="@color/gold_70"
                    android:textStyle="bold"
                    android:text="@string/artist_urls"/>
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginVertical="@dimen/margin_normal"
                    android:paddingHorizontal="@dimen/padding_medium"
                    android:textSize="@dimen/font_normal"
                    android:textColor="@color/white"
                    android:lineSpacingExtra="@dimen/font_normal"
                    android:autoLink="web"
                    android:textColorLink="@color/white"
                    app:textList="@{viewModel.artistInfo.urls}"
                    app:separator="@{String.valueOf('\n')}"
                    tools:text="artist profile"/>
            </androidx.appcompat.widget.LinearLayoutCompat>
            </androidx.appcompat.widget.LinearLayoutCompat>
        </androidx.core.widget.NestedScrollView>
    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>