<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="android.view.View"/>
        <import type="de.hicedevelopments.princemusicapp.ui.about.AboutViewModel.AboutEvent"/>
        <variable
            name="viewModel"
            type="de.hicedevelopments.princemusicapp.ui.about.AboutViewModel" />
    </data>
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/coordinator"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/purple">
        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appbar"
            android:theme="@style/Widget.AppTheme.AppBar"
            android:background="@color/status_bar_scrim"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <com.google.android.material.appbar.CollapsingToolbarLayout
                android:id="@+id/collapsing_toolbar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:contentScrim="@color/purple"
                app:expandedTitleGravity="bottom"
                app:expandedTitleTextColor="@color/gold_70"
                app:expandedTitleTextAppearance="@style/TextAppearance.AppCompat.Headline"
                app:collapsedTitleTextColor="@color/gold"
                app:title="@{viewModel.aboutModel.title}"
                tools:title="Artist name"
                app:titleCollapseMode="fade"
                app:layout_scrollFlags="scroll|exitUntilCollapsed">
                <androidx.constraintlayout.widget.ConstraintLayout
                    android:id="@+id/header_container"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:layout_collapseMode="parallax">
                    <androidx.appcompat.widget.AppCompatImageView
                        android:id="@+id/header"
                        android:layout_width="match_parent"
                        android:layout_height="0dp"
                        android:scaleType="centerCrop"
                        app:imageUrl="@{viewModel.aboutModel.images.get(0).uri}"
                        app:showPlaceholder="@{false}"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintDimensionRatio="H, 1:1"/>
                </androidx.constraintlayout.widget.ConstraintLayout>
                <androidx.constraintlayout.widget.ConstraintLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:onClick="@{() -> viewModel.onEvent(AboutEvent.IMAGE_CLICK)}">
                    <View
                        android:layout_width="match_parent"
                        android:layout_height="0dp"
                        app:layout_constraintDimensionRatio="H, 1:1"
                        android:background="@drawable/release_detail_title_bg"
                        app:layout_constraintBottom_toBottomOf="parent"/>
                    <androidx.appcompat.widget.AppCompatImageView
                        android:id="@+id/imv_zoom"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_margin="@dimen/margin_large"
                        android:visibility="@{viewModel.aboutModel.images.size() > 1 ? View.VISIBLE : View.GONE}"
                        app:layout_constraintBottom_toBottomOf="parent"
                        app:layout_constraintEnd_toEndOf="parent"
                        android:src="@drawable/ic_zoom_image" />
                </androidx.constraintlayout.widget.ConstraintLayout>
                <androidx.appcompat.widget.Toolbar
                    android:id="@+id/toolbar"
                    android:layout_width="match_parent"
                    android:layout_height="?attr/actionBarSize"
                    android:elevation="@dimen/toolbar_elevation"
                    android:theme="@style/PrinceToolbar"
                    app:useAsBackButton="@{true}"
                    app:navigationIcon="@drawable/ic_circular_back"
                    app:layout_collapseMode="pin"/>
            </com.google.android.material.appbar.CollapsingToolbarLayout>
        </com.google.android.material.appbar.AppBarLayout>
        <androidx.core.widget.NestedScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:overScrollMode="ifContentScrolls"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">
            <androidx.appcompat.widget.LinearLayoutCompat
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">
                <androidx.appcompat.widget.LinearLayoutCompat
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingBottom="@dimen/padding_normal"
                    app:layout_constraintTop_toTopOf="parent"
                    android:orientation="vertical"
                    android:visibility="@{viewModel.aboutModel.realName == null ? View.GONE : View.VISIBLE}">
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:background="@drawable/release_video_title_bg"
                        android:layout_marginVertical="@dimen/margin_normal"
                        android:padding="@dimen/padding_medium"
                        android:textSize="@dimen/font_large"
                        android:textColor="@color/gold_70"
                        android:textStyle="bold"
                        android:lineSpacingExtra="0sp"
                        android:text="@string/about_real_name"/>
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginVertical="@dimen/margin_normal"
                        android:layout_marginHorizontal="@dimen/padding_medium"
                        android:textSize="@dimen/font_normal"
                        android:textColor="@color/white"
                        android:text="@{viewModel.aboutModel.realName}"
                        tools:text="artist real name"/>
                </androidx.appcompat.widget.LinearLayoutCompat>
                <androidx.appcompat.widget.LinearLayoutCompat
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingBottom="@dimen/padding_normal"
                    app:layout_constraintTop_toTopOf="parent"
                    android:orientation="vertical"
                    android:visibility="@{viewModel.aboutModel.realName == null ? View.GONE : View.VISIBLE}">
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:background="@drawable/release_video_title_bg"
                        android:layout_marginVertical="@dimen/margin_normal"
                        android:padding="@dimen/padding_medium"
                        android:textSize="@dimen/font_large"
                        android:textColor="@color/gold_70"
                        android:textStyle="bold"
                        android:lineSpacingExtra="0sp"
                        android:text="@string/about_name_variations"/>
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginVertical="@dimen/margin_normal"
                        android:layout_marginHorizontal="@dimen/padding_medium"
                        android:textSize="@dimen/font_normal"
                        android:textColor="@color/white"
                        app:textList="@{viewModel.aboutModel.nameVariations}"
                        tools:text="artist name variants"/>
                </androidx.appcompat.widget.LinearLayoutCompat>
                <androidx.appcompat.widget.LinearLayoutCompat
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingBottom="@dimen/padding_normal"
                    app:layout_constraintTop_toTopOf="parent"
                    android:orientation="vertical"
                    android:visibility="@{viewModel.aboutModel.profile == null ? View.GONE : View.VISIBLE}">
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:background="@drawable/release_video_title_bg"
                        android:layout_marginVertical="@dimen/margin_normal"
                        android:padding="@dimen/padding_medium"
                        android:textSize="@dimen/font_large"
                        android:textColor="@color/gold_70"
                        android:textStyle="bold"
                        android:text="@string/about_profile"/>
                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginVertical="@dimen/margin_normal"
                        android:paddingHorizontal="@dimen/padding_medium"
                        android:textSize="@dimen/font_normal"
                        android:textColor="@color/white"
                        android:text="@{viewModel.aboutModel.profile}"
                        tools:text="artist profile"/>
                </androidx.appcompat.widget.LinearLayoutCompat><androidx.appcompat.widget.LinearLayoutCompat
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingBottom="@dimen/padding_normal"
                app:layout_constraintTop_toTopOf="parent"
                android:orientation="vertical"
                android:visibility="@{viewModel.aboutModel.links == null ? View.GONE : View.VISIBLE}">
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@drawable/release_video_title_bg"
                    android:layout_marginVertical="@dimen/margin_normal"
                    android:padding="@dimen/padding_medium"
                    android:textSize="@dimen/font_large"
                    android:textColor="@color/gold_70"
                    android:textStyle="bold"
                    android:text="@string/about_urls"/>
                <androidx.recyclerview.widget.RecyclerView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                    app:items="@{viewModel.aboutModel.links}"
                    app:itemBinding="@{viewModel.webLinkBinding}"
                    tools:listitem="@layout/item_weblink"/>
            </androidx.appcompat.widget.LinearLayoutCompat>
            </androidx.appcompat.widget.LinearLayoutCompat>
        </androidx.core.widget.NestedScrollView>
    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>